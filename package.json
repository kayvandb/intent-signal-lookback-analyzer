{
  "name": "intent-signal-lookback-analyzer",
  "private": true,
  "version": "1.0.0",
  "description": "Lookback analysis that finds which intent topics genuinely preceded pipeline creation, then scores current accounts by surge propensity on the topics that held up.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "papaparse": "^5.4.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
